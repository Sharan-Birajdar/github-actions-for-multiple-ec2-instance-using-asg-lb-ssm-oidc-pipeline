name: Deploy to ASG via SSM (OIDC)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-south-1
  ASG_NAME: ${{ secrets.ASG_NAME }}
  TARGET_GROUP_ARN: ${{ secrets.TARGET_GROUP_ARN }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 🔐 OIDC Authentication
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # 🔍 Get Running Instances from ASG
      - name: Get Instances in ASG
        id: instances
        run: |
          IDS=$(aws ec2 describe-instances \
            --filters "Name=tag:aws:autoscaling:groupName,Values=$ASG_NAME" \
                      "Name=instance-state-name,Values=running" \
            --query "Reservations[*].Instances[*].InstanceId" \
            --output text | tr '\n' ' ')

          if [ -z "$IDS" ]; then
            echo "❌ No running instances found!"
            exit 1
          fi

          echo "Instances: $IDS"
          echo "ids=$IDS" >> $GITHUB_OUTPUT

      # 🔁 Safe Rolling Deployment
      - name: Rolling Deploy using SSM (Safe)
        run: |
          set -e

          for INSTANCE in ${{ steps.instances.outputs.ids }}
          do
            echo "======================================"
            echo "🚀 Processing $INSTANCE"
            echo "======================================"

            # ✅ Check instance state
            STATE=$(aws ec2 describe-instances \
              --instance-ids "$INSTANCE" \
              --query "Reservations[0].Instances[0].State.Name" \
              --output text 2>/dev/null || echo "terminated")

            if [ "$STATE" != "running" ]; then
              echo "❌ Skipping $INSTANCE (state: $STATE)"
              continue
            fi

            # ✅ Check SSM availability
            SSM_STATUS=$(aws ssm describe-instance-information \
              --query "InstanceInformationList[?InstanceId=='$INSTANCE'].PingStatus" \
              --output text)

            if [ "$SSM_STATUS" != "Online" ]; then
              echo "❌ Skipping $INSTANCE (SSM not ready)"
              continue
            fi

            # ⚡ Reduce deregistration delay
            aws elbv2 modify-target-group-attributes \
              --target-group-arn "$TARGET_GROUP_ARN" \
              --attributes Key=deregistration_delay.timeout_seconds,Value=30

            # ✅ Standby (FIXED)
            echo "➡️ Putting instance into standby..."
            aws autoscaling enter-standby --instance-ids "$INSTANCE" --auto-scaling-group-name "$ASG_NAME" --should-decrement-desired-capacity false

            # ⏳ Wait for Standby
            echo "⏳ Waiting for Standby..."
            for i in $(seq 1 20); do
              STATE=$(aws autoscaling describe-auto-scaling-instances \
                --instance-ids "$INSTANCE" \
                --query "AutoScalingInstances[0].LifecycleState" \
                --output text)
              echo "State: $STATE"
              [ "$STATE" = "Standby" ] && break
              sleep 10
            done

            # 🚀 DEPLOY (FIXED PART)
            echo "⚙️ Deploying application..."
            COMMAND_ID=$(aws ssm send-command \
              --instance-ids "$INSTANCE" \
              --document-name "AWS-RunShellScript" \
              --query "Command.CommandId" \
              --parameters commands='[
                "echo ===== START DEPLOY =====",
                "whoami",
                "hostname",
                "sudo mkdir -p /var/www/html",
                "sudo chown -R ubuntu:ubuntu /var/www/html",
                "cd /var/www/html",
                "echo BEFORE:",
                "ls -la",
                "if [ -d \".git\" ]; then git reset --hard && git pull origin main; else git clone https://github.com/YOUR_USERNAME/YOUR_REPO .; fi",
                "echo AFTER:",
                "ls -la",
                "sudo systemctl restart nginx",
                "echo ===== END DEPLOY ====="
              ]' \
              --output text)

            aws ssm wait command-executed \
              --command-id "$COMMAND_ID" \
              --instance-id "$INSTANCE"

            # 📋 Show logs
            aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$INSTANCE" \
              --query "{Status: Status, Output: StandardOutputContent, Error: StandardErrorContent}" \
              --output text

            # 🔁 Restore delay
            aws elbv2 modify-target-group-attributes \
              --target-group-arn "$TARGET_GROUP_ARN" \
              --attributes Key=deregistration_delay.timeout_seconds,Value=300

            # ⬅️ Exit standby
            echo "⬅️ Bringing instance back..."
            aws autoscaling exit-standby \
              --instance-ids "$INSTANCE" \
              --auto-scaling-group-name "$ASG_NAME"

            # ⏳ Wait InService
            echo "⏳ Waiting for InService..."
            for i in $(seq 1 20); do
              STATE=$(aws autoscaling describe-auto-scaling-instances \
                --instance-ids "$INSTANCE" \
                --query "AutoScalingInstances[0].LifecycleState" \
                --output text)
              echo "State: $STATE"
              [ "$STATE" = "InService" ] && break
              sleep 10
            done

            echo "✅ $INSTANCE deployed successfully"
          done

